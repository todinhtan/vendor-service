const COOP_STATUSES = {
  available: 'available',
  pending: 'pending',
  sold: 'sold',
};

const FILTER_TYPES = {
  act: 'act',
  noAct: 'noAct',
};

module.exports = {
  COOP_STATUSES,
  FILTER_TYPES,
};
